"use client";

import PageHero from "@/components/common/PageHero";

const Hero = () => {
  return (
    <PageHero

      image="/contact _hero2.png"
      imageAlt="Contact Vontrixia"
      breadcrumb={["Home", "Contact"]}
    />
  );
};

export default Hero;